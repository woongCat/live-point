# live-point 설계 문서

## 개요

**live-point**는 개인용 음성 메모 웹앱으로, 실시간 음성 전사와 함께 "진짜 하고 싶은 말(요지)"을 자동 추출한다.

## 핵심 컨셉

- **가운데**: 실시간 전사 (더듬거림, 필러 포함)
- **우측**: 요지 추출 (핵심 의도만)
- **좌측**: 과거 세션 기록

## 기술 스택

| 영역 | 기술 | 이유 |
|------|------|------|
| Backend | Python + FastAPI | ML 생태계, WebSocket 지원 |
| STT | faster-whisper (로컬) | 무료, 프라이버시, 한국어 지원 |
| LLM | OpenAI GPT-4o-mini | 빠른 응답, 스트리밍, 저렴 |
| Frontend | React + TypeScript + Vite | 빠른 개발, 타입 안정성 |
| 스타일링 | Tailwind CSS | 빠른 프로토타이핑 |
| 상태관리 | Zustand | 간단, 보일러플레이트 적음 |
| 로컬저장 | IndexedDB (Dexie.js) | 대용량 세션 저장 |

## 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                        Browser (React)                          │
│  ┌──────────┐    ┌──────────────┐    ┌───────────────────────┐ │
│  │ Mic API  │───▶│ AudioWorklet │───▶│ WebSocket Client      │ │
│  └──────────┘    │ (500ms chunk)│    │ (binary audio)        │ │
│                  └──────────────┘    └───────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼ WebSocket
┌─────────────────────────────────────────────────────────────────┐
│                     FastAPI Backend                              │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────┐ │
│  │ WebSocket       │───▶│ faster-whisper  │───▶│ LLM Client  │ │
│  │ Handler         │    │ (2-3s buffer)   │    │ (streaming) │ │
│  └─────────────────┘    └─────────────────┘    └─────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## 데이터 흐름

1. **Audio Capture**: 브라우저 MediaRecorder → 16kHz mono PCM → 500ms 청크
2. **WebSocket**: 청크를 바이너리로 서버 전송
3. **Whisper**: 2-3초 버퍼 누적 후 전사, VAD로 묵음 스킵
4. **Pause Detection**: 1.5초 침묵 감지 시 해당 구간 텍스트를 LLM으로
5. **Point Extraction**: LLM이 요지 추출 → 클라이언트로 스트리밍
6. **UI Update**: 가운데에 전사 추가, 우측에 요지 카드 생성

## UI 레이아웃

```
┌──────────────────────────────────────────────────────────────────────────┐
│  live-point                                               [🎤 녹음중]    │
├──────────────────────────────────────────────────────────────────────────┤
│   과거 기록        │      실시간 전사             │   요지 (The Point)   │
│                    │                              │                      │
│  ┌──────────────┐  │  전사 텍스트가 여기에       │  ┌────────────────┐  │
│  │ 날짜/시간    │  │  실시간으로 흘러내림        │  │ 추출된 요지가  │  │
│  │ 세션 제목    │  │                              │  │ 여기에 표시    │  │
│  │ • N points   │  │  pause 감지 시 ~~~>         │  │                │  │
│  └──────────────┘  │  텍스트가 우측으로 이동     │  └────────────────┘  │
│                    │                              │                      │
│  [+ 새 세션]       │                              │   [복사] [내보내기]  │
└──────────────────────────────────────────────────────────────────────────┘
```

## 프로젝트 구조

```
live-point/
├── backend/
│   ├── main.py              # FastAPI 진입점
│   ├── websocket.py         # WebSocket 핸들러
│   ├── whisper_service.py   # faster-whisper 래퍼
│   ├── llm_service.py       # OpenAI/Claude 클라이언트
│   ├── point_extractor.py   # 요지 추출 로직
│   └── requirements.txt
│
├── frontend/
│   ├── src/
│   │   ├── App.tsx
│   │   ├── components/
│   │   │   ├── HistoryPanel.tsx    # 좌측 과거 기록
│   │   │   ├── TranscriptFlow.tsx  # 가운데 전사
│   │   │   └── PointPanel.tsx      # 우측 요지
│   │   ├── hooks/
│   │   │   ├── useAudioCapture.ts  # 마이크 캡처
│   │   │   └── useWebSocket.ts     # WS 연결
│   │   └── stores/
│   │       └── sessionStore.ts     # 세션 상태
│   ├── package.json
│   └── vite.config.ts
│
├── docs/plans/
├── CLAUDE.md
└── README.md
```

## LLM 프롬프트 (요지 추출)

```
사용자가 말한 내용에서 핵심 의도만 추출해.
- 필러(음, 그러니까, 뭐랄까)와 반복 제거
- 진짜 하고 싶은 말을 1-2문장으로
- 한국어로 응답

입력: "{transcript_chunk}"
```

## 예상 지연시간

| 구간 | 예상 시간 |
|------|-----------|
| Audio → Server | ~50ms |
| Whisper 처리 | 1-2초 (M1/M2 Mac) |
| LLM 응답 시작 | ~500ms |
| **총 지연** | **2-3초** |

## 데이터 모델

```typescript
interface Session {
  id: string;
  createdAt: Date;
  title: string;          // 자동 생성 또는 수동
  transcript: string;     // 전체 전사
  points: Point[];
}

interface Point {
  id: string;
  timestamp: Date;
  sourceText: string;     // 원본 전사 구간
  point: string;          // 추출된 요지
}
```
